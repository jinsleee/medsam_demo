# Web framework
Flask==3.1.3

# Image processing
Pillow==12.2.0
numpy==2.2.6
opencv-python==4.13.0.92
matplotlib==3.10.8

# PyTorch (CPU 버전 — GPU 사용 시 아래 주석 참고)
# CPU:
torch==2.11.0+cpu
torchvision==0.26.0+cpu
# GPU (CUDA 11.8):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# MedSAM (segment_anything 포함)
# pip install -e MedSAM/
